import clamp from "./clamp"
import defineRandomHexColor from "./defineRandomHexColor"
import defineRandomCountdown from "./defineRandomCountdown"

export {
    clamp,
    defineRandomHexColor,
    defineRandomCountdown
}